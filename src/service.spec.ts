import { Service } from "./service";

describe('service', () => {
    let service: Service

    beforeAll(() => {
        service = new Service()
    })

    describe('when this', () => {
        it('returns that', () => {

        })
    })
})